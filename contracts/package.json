{
  "name": "evnto-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Evnto event management platform on Monad",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:monad": "hardhat run scripts/deploy.js --network monad-testnet",
    "verify": "hardhat verify --network monad-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
